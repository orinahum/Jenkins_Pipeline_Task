
pipeline {
    // agent {label 'workers'}
    node ('worker2')
    stages {
        stage('Spellcheck') {
            steps {
                sh '''
                chmod +x scripts/spellcheck.sh
                bash scripts/spellcheck.sh
                '''
            }
        }
    }
}
