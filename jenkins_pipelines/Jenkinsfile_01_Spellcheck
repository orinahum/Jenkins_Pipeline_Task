
pipeline {
    // agent {label 'workers'}
    agent {label 'worker-2'}
    stages {
        stage('Spellcheck') {
            steps {
                sh '''
                chmod +x scripts/spellcheck.sh
                bash scripts/spellcheck.sh
                '''
            }
        }
    }
}
