
pipeline {
    agent {label 'workers'}
    stages {
        stage('Spellcheck') {
            steps {
                sh 'chmod +x /tmp/Details_App/scripts/spellcheck.sh'
                sh '/tmp/Details_App/scripts/spellcheck.sh'
            }
        }
    }
}
