
pipeline {
    agent {label 'workers'}
    stages {
        stage('Spellcheck') {
            steps {
                sh 'curl -u admin:admin "http://localhost/job/pipeline_manager/build?token=spellcheck"'
            }
        }
    }
}
